apiVersion: v1
kind: ResourceQuota
metadata:
  name: demo-quota
  namespace: demo
spec:
  hard:
    pods: "2"
    requests.cpu: "100m"
    requests.memory: 256Gi
    limits.cpu: "300m"
    limits.memory: 512Gi
